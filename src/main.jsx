import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")).render(
  <div style={{
    padding: "40px",
    fontSize: "30px",
    color: "red",
    direction: "rtl"
  }}>
    ARZAQ TEST — React يعمل
  </div>
);
